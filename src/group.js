;(function() {
    'use strict';
    
    window.Group = Group;
    
    function Group(options)
    {
        if (!(this instanceof Group))
        {
            return new Group(options);
        }
        
        var me = this;
        
        function init()
        {
        }

        init();
        
        return this;
    }
})();